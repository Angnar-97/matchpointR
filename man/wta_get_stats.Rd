% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stats.R
\name{wta_get_stats}
\alias{wta_get_stats}
\title{Get WTA tour statistics leaderboards}
\usage{
wta_get_stats(metric = "aces", top = NULL)
}
\arguments{
\item{metric}{Character. Stat slug as used by the WTA site (e.g.
\code{"aces"}, \code{"first-serve-percentage"}, \code{"break-points-converted"},
\code{"winners"}, \code{"unforced-errors"}). The full path is
\verb{https://www.wtatennis.com/stats/<metric>}.}

\item{top}{Integer. Limit to the top \code{N} rows. \code{NULL} (default) returns
every row rendered by the site.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with columns: \code{rank}, \code{player}, \code{country},
\code{value}.
}
\description{
Scrapes the public stats hub at
\url{https://www.wtatennis.com/stats} and returns the leaderboard for the
requested metric as a tidy tibble.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
wta_get_stats("aces", top = 25)
\dontshow{\}) # examplesIf}
}
